#include "main.h"
#include <stdio.h>
/**
 * _abs- Absolute value of integer.
 *@d: Parameter to be checked.
 * Return: Always 0
 */
int _abs(int)
{
	if (d < 0)
	{
		return (-d);
	}
	else
	{
		return (d);
	}
}
