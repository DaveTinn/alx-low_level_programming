#include "main.h"
#include <stdio.h>
/**
 * print_to_98- Function to print natural numbers.
 * @n: Parameter to be checked.
 * Return: Always 0
 */
void print_to_98(int n)
{
	int d;
		if (d = 0; d <= 98; d++)
		{
			_putchar(d + '0');
			_putchar(',');
			_putchar(' ');
		}
		_putchar('\n');
	}
	else if (n < 0)
	{
		for (d = n; d <= 98; d++)
		{
			_putchar(d + '0');
			_putchar(',');
			_putchar(' ');
		}
		_putchar('\n');
	}
	else if (n > 98)
	{
		for (d = n; d >= 98; d--)
		{
		       _putchar(d + '0');
		       _putchar(',');
		       _putchar(' ');
		}
		_putchar('\n');
	}
	else if (n == 98)
}
