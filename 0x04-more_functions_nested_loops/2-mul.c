#include "main.h"
/**
 * mul - Function to multiply two integers.
 * @a: Parameter for multiplication.
 * @b: Parameter to be multiplication.
 *
 * Return: a * b.
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
