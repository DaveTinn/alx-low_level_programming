#include "main.h"

/**
 * mul - Function to multiply two integers. 
 * @a: character to be multiplied
 * @b: character to be multiplied
 *
 * Return: a * b.
 */
int mul(int a, int b)
{
	return (a * b);
}
