#include "main.h"

/**
 * mul - Function to multiply two integers. 
 * @d: Parameter for multiplication.
 * @e: Parameter to be multiplication.
 *
 * Return: d * e.
 */
int mul(int a, int b)
{
	int product;

	product = d * e;
	return (product);
}
