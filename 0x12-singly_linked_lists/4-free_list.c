#include "lists.h"
/**
 * free_list - free a list_t list
 * @head: pointer to a list
 *
 * Return: free
 */
void free_list(list_t *head)
{
	{
        size_t element = 0;

        while (h != NULL)
        {
                if (h->str == NULL)
                {
                h = h->next;
                element++;
        }
        return (element);
}
